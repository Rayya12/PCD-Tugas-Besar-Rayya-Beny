# ================================
# Web Framework
# ================================
fastapi==0.115.5
uvicorn[standard]==0.32.1          # ASGI server with websocket & hot-reload support
python-multipart==0.0.12           # Required for file upload (multipart/form-data)

# ================================
# Image Processing (Core)
# ================================
numpy==2.1.3                       # Array operations, semua operasi gambar berbasis ini
Pillow==11.0.0                     # Baca/tulis format gambar (PNG, JPEG, BMP, TIFF, dll)
opencv-python-headless==4.10.0.84  # Edge detection, morphological ops, color space conv
                                   # Headless = tanpa GUI dependency (cocok untuk server)
scipy==1.14.1                      # Histogram specification (interpolasi), filter lanjutan

# ================================
# Data Validation & Serialization
# ================================
pydantic==2.10.1                   # Schema validation untuk request/response (sudah include di FastAPI)

# ================================
# Async & Performance
# ================================
anyio==4.6.2                       # Async support untuk FastAPI
httpx==0.27.2                      # HTTP client async (opsional, untuk testing)

# ================================
# CORS & Middleware
# ================================
# (sudah include di FastAPI via starlette)

# ================================
# Optional: Background Tasks (jika operasi berat)
# ================================
# celery==5.4.0                    # Uncomment jika butuh background job queue
# redis==5.2.0                     # Broker untuk Celery

# ================================
# Development & Testing
# ================================
pytest==8.3.3
pytest-asyncio==0.24.0             # Testing async FastAPI endpoints
httpx==0.27.2                      # Test client untuk FastAPI (via TestClient)
python-dotenv==1.0.1               # Load environment variables dari .env

# ================================
# Optional: Image Format Tambahan
# ================================
# imageio==2.36.1                  # Uncomment jika butuh support format seperti TIFF, GIF animasi
# rawpy==0.22.0                    # Uncomment jika butuh support RAW camera format
